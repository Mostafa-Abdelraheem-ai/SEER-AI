# Leave empty to use the same origin through the bundled Nginx reverse proxy.
# Set this only if you want the frontend to call a different public backend URL directly.
VITE_API_BASE_URL=
FRONTEND_PORT=80
